algoritmo "ExemploRepita"
var
   numero: inteiro  // Variável para armazenar o número inserido pelo usuário
inicio
   // Laço que repete até que o número digitado seja 0
   repita
      escreva("Digite um número (0 para sair): ")  // Pede ao usuário um número
      leia(numero)                                 // Lê o número inserido
   ate numero = 0                                  // Sai do laço quando o número for 0
fimalgoritmo
